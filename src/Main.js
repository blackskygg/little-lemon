const mainClasses = [
    'grid content-center justify-center h-32',
].join(' ');

function Main() {
    return (
        <main className={mainClasses}>
            main
        </main>
    );
}

export default Main;